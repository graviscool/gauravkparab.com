/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "github-readme-stats-orpin-seven-78.vercel.app",
      },
    ],
  },
};

export default nextConfig;
